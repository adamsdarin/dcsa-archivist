interface:
  display_name: "DCSA Library Custodian"
  short_description: "Audit and release DCSA retrieval libraries"
  default_prompt: "Use $dcsa-library-custodian to audit my DCSA Library and prepare a preview-only release candidate."
policy:
  allow_implicit_invocation: true
